/** sessionStorage key for AgentTerminal / AgentChat persisted transcript (avoid importing from `use-agent-intelligence` — circular with `local-workspace-reset`). */
export const AGENT_CHAT_STORAGE_KEY = 'projectr-agent-chat-v1'
